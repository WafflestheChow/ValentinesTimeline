# JAN assets

How to add your photos:
1) Put images into `src/assets/jan/` with names like `jan-01.jpg`, `jan-02.jpg`, etc.
2) Either keep the string-path approach in `src/data/timeline.ts` OR uncomment the explicit import lines and point `images[].src` to the imported modules.
